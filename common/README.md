
# Common Utilities

This folder contains shared utilities for the microservices lab.

## ids.py

Provides helper functions for generating UUIDs:
- `generate_order_id()`: Generates a unique order ID.
- `generate_correlation_id()`: Generates a unique correlation ID for tracing requests across services.
